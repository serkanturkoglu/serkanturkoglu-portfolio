# Production domain — used for the sitemap, robots.txt, canonical URLs,
# and Open Graph tags. Set this in your hosting provider's environment
# variables (e.g. Vercel project settings) once the real domain is live.
SITE_URL=https://www.serkanturkoglu.com
